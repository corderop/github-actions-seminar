name: "Testing"
on:
  push:
    branches:
      - "tests"
  pull_request:

jobs:
  Testing:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: ./
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          ban-file: ".github/ban_list"
